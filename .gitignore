# ignore package-lock
package-lock.json

# ignore package.json
package.json

# ignore the pesky node_modules directory
node_modules/

# ginore the notes data file, so everyone can have their own notes
notes-data.json